#spring:
#  application:
#    name: api-client1
#
#server:
#  port: 1001
#
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://eureka.springcloud.cn/eureka/
spring:
  application:
    name: api-client1
  cloud:
    config:
      profile: dev
      label: master
#      uri: http://localhost:1004
#      如果配置中心已经注册则可以用服务名
      discovery:
        enabled: true
        service-id: config

server:
  port: 1001

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.springcloud.cn/eureka/